{% extends 'base.html.twig' %}

{% block title %}
    {{ build.title }}
{% endblock %}

{% block banner %}
<div class="game-banner">
    <img
        src="/uploads/banners/{{ build.game.bannerImage }}"
        alt="{{ build.game.name }}"
    >
</div>
{% endblock %}

{% block body %}

<div class="build-show">

    <h1>{{ build.title }}</h1>

    <div class="build-meta">

        <span>🎮 {{ build.game.name }}</span>

        <span>👤 {{ build.user.username }}</span>

        <span>📅 {{ build.createdAt|date('d/m/Y H:i') }}</span>

    </div>

    {% if build.image %}

        <div class="build-image">

            <img
                src="/uploads/builds/{{ build.image }}"
                alt="{{ build.title }}"
            >

        </div>

    {% endif %}

    <div class="build-description">

        {{ build.description|nl2br }}

    </div>

    <br>

    <a
        class="btn-primary"
        href="{{ path('game_show', {'slug': build.game.slug}) }}"
    >
        ← Volver al juego
    </a>

</div>

{% endblock %}